{{#partial "content"}}
    <h1>List of all DIT Staff</h1>

    {{# if staff}}
        {{#each staff }}
            <p><a href="/departments/{{departmentsId}}/staff/{{id}}">{{description}}</a> </p>
        {{/each}}
    {{else}}
        <p>No staff</p>
    {{/if}}
{{/partial}}

{{> layout.hbs}}