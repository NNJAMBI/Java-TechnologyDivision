{{#partial "content"}}

    <h1>List of all Staff</h1>

    {{# if staff}}
        {{#each staff }}
            <p><a href="/departments/{{departmentId}}/staff/{{id}}">{{description}}</a> </p>
        {{/each}}
    {{else}}
        <p>No staff added</p>
    {{/if}}

{{/partial}}

{{> layout.hbs}}